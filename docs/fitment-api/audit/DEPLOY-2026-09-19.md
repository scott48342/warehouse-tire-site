# Fitment audit fix branch - blockers & deployment procedure (2026-09-19 10:50 ET)

Branch: `fix/fitment-audit-2026-09-18` @ `HEAD` (see `git log -1`; doc written at `b332088c`) (worktree `F:\clawd\wt-fitment-fix`), 31 commits on top of
`main` @ `4742a0e2` (which itself is 16 commits ahead of `origin/main`, unpushed). Separate, NOT to deploy yet:
`h5-option-a-runtime` @ `f65f0e9a` (needs migration 0051 first).

## Remaining blockers (actual)

| # | Blocker | Owner | What unblocks |
|---|---|---|---|
| 1 | **Reviewer acceptance** of the Jake batch (`40860ce8`/`8c3b76f2`) and of today's commits (`09e18766` stagger/load-scope, `f6db6174` packages, `b332088c` tone). Scott's browser pass so far: J2/J4 refusals PASS, Mach-E chips PASS, HD 6x139.7 rejection PASS, tone "still verbose" (addressed in `b332088c`, untested). | Scott | Scott's "accepted" |
| 2 | **Nothing is pushed.** `main` is 16 ahead of origin; the fix branch has never been pushed. Vercel builds from GitHub. | Scott (declined to delegate pushes) | `git push origin main` + push the branch (see procedure) |
| 3 | ~~Jake "check the door jamb for bolt pattern"~~ - fixed in the commit after `0f83d535` (prompt: placard = tire size/PSI; bolt pattern = measure wheel / dealer VIN / bring it in). Retest by Scott. | Scott | browser retest |
| 4 | **CORRECTED 10:20 (DB auditor + read-only verify):** the 4 rows are NOT EV contamination. `463d44a3`, `6dc2e084`, `b6dce2ba` are `model = mustang-mach-1` (gas Mach 1: 5x114.3, 255/40R19, Handling Package 305/30R19 - plausible; sources catalog-gap-fill / verified-research / google-ai-overview, unverified). Only `8302a4ef` is `mustang-mach-e` Base (225/55R19, 225/60R18, 245/45R20) and its **bolt 5x114.3 is suspect (Mach-E = 5x108)**; source cache-import, 0 approved sources -> flag only. OPEN CODE QUESTION: why did the Mach-E lookup surface Mach 1 rows as 'Base/Handling Package' - alias/prefix match on `mustang-mach`? Check resolver before deploy. | me | resolver check |
| 5 | **H5 Option A decision** - migration 0051 + backfill (28,268 rows) + runtime branch. Fully prepared; nothing applied. Until then the interim `per_axle_unknown` rule (live on the branch) keeps every multi-size/staggered vehicle package-ineligible with no minimum shown - safe, but it removes package recommendations from ~16k rows. | Scott | "go" on the 5-step procedure in `h5-load-index-provenance.md` |
| 6 | **Perf, not correctness:** `/wheels?…&package=1` on the M4 renders 782 staggered pairs (`pageSize=2000`), 31 s server render, 1.3 MB HTML. Pre-existing page design; H2 pairing made every finish a pair. Suggest a follow-up: cap pairs per style or paginate. | later | separate task |
| 7 | Jest baseline: 11 suites fail identically on `4742a0e2` (vitest-in-jest imports, packageGap ±5 mm bound, etc.). Not introduced here; not fixed here. | later | separate cleanup |

Not blockers: `packages/recommended` returns 0 packages for source-unverified vehicles (Camry, Raptor) - that is the
R3 gate from `1e4281de` working as designed; Mach-E GT (TG Pro) builds one, flagged estimated/not certified.

## What the branch changes at runtime (for the deploy risk read)
- Fit certification fails closed on: trim ambiguity, unapproved source provenance, mixed-diameter requests on
  square/unknown OE vehicles, non-OE sizes (`direct:`, plus-sizing, lifted, classic upsize).
- Load-index minimum asserted only for single-OE-size records; otherwise no minimum, package-ineligible.
- Staggered wheels: same-finish front/rear pairing, 2+2 pricing everywhere, both axles handed to tire search.
- Packages: estimated/never certified/never directly purchasable; LT-aware; no NaN/fabricated sizes.
- Copy: no "Guaranteed"/"Fits" without `fitBadgeAllowed`; Jake refuses to "confirm" unverified specs.
- Cache: fitment profile cache key **v7** (v8 only on the H5 runtime branch) - old entries are simply missed, no flush needed.
- **No new env vars for production.** `FITMENT_PREVIEW_READONLY` / `FITMENT_CACHE_DISABLED` are preview-only switches, unset in Vercel = off.
- **No migration required** for the fix branch. 0051 is on the branch as a file only; nothing reads the new columns until `h5-option-a-runtime` merges.
- No supplier/order-path code touched (Stripe webhook, USAF/WheelPros order clients untouched - `git diff 4742a0e2..HEAD --stat -- src/lib/suppliers src/lib/usautoforce src/lib/wheelpros/orderClient.ts src/app/api/webhooks` is empty).

## Exact deployment procedure (after Scott's acceptance; standing deploy authorization applies)

Targets: GitHub `scott48342/warehouse-tire-site`, Vercel project `warehouse-tire-site` -> production domains
`shop.warehousetiredirect.com`, `shop.warehousetire.net`, `pos.warehousetiredirect.com` (one project, deploys from `main`).

1. **Final gate in the worktree** (serial, ~4 min):
   ```powershell
   cd F:\clawd\wt-fitment-fix
   npx tsc --noEmit -p tsconfig.json          # expect exit 0 (last: 0 at b332088c parent)
   npx jest --silent 2>&1 | Select-String "Tests:"   # expect 19 failed (baseline) / rest pass
   npm run build                                # DONE 2026-09-19 10:05 @80084d2c: EXIT 0, 533 s, 1332 static pages; log docs/fitment-api/audit/next-build-80084d2c.log
   ```
2. **Merge into main** (fast-forward is impossible - branch is based on `4742a0e2` = main HEAD, so it IS a fast-forward):
   ```powershell
   cd F:\clawd\warehouse-tire-site
   git status --short        # must be clean of unrelated edits, or stash them (Scott's call - main has TG-loop work)
   git merge --ff-only fix/fitment-audit-2026-09-18
   git log --oneline -1      # b332088c
   ```
3. **Push** (Scott): `git push origin main`. Vercel auto-builds production from `main`. Watch the build in the Vercel dashboard; a failed build leaves the previous deployment live.
4. **Post-deploy smoke (5 min, read-only)** against `https://shop.warehousetiredirect.com`:
   - `/api/tires/search?year=2022&make=Toyota&model=Camry&modification=SE&wheelDiameter=18&rearWheelDiameter=20&pageSize=5` -> `certificationBlock: "aftermarket_stagger"`, `staggeredSearch.oemStaggered: false`.
   - `/api/tires/search?year=2020&make=Chevrolet&model=Corvette&modification=Stingray&wheelDiameter=19&rearWheelDiameter=20&pageSize=5` -> `oemStaggered: true`, `loadRequirement.scope: "per_axle_unknown"`, every result `packageEligible: false`.
   - `/api/tires/search?year=2024&make=Ford&model=Mustang&modification=GT&wheelDiameter=19&rearWheelDiameter=20&pageSize=5` -> `certificationBlock: "stagger_unverified"`.
   - `/api/wheels/fitment-search?year=2024&make=BMW&model=M4&trim=Competition%20xDrive&package=1&pageSize=50` -> pairs have `front.finish === rear.finish`, `setPrice === 2*front.price + 2*rear.price`.
   - `/api/packages/recommended?year=2022&make=Ford&model=Mustang%20Mach-E&trim=GT` -> `packages[0].priceEstimated === true`, `certified === false`.
   - `/api/fitment/lifted?year=2020&make=Ford&model=F-150&trim=XLT&liftInches=4` -> `liftLevel.id: "offroad"`.
   - Jake: one chat "2024 BMW M4 Competition xDrive bolt pattern?" -> must not say "confirmed"/"both sources".
   - `npm run fitment:smoke-test` (existing production smoke) if it still passes on baseline.
5. **Rollback**: Vercel dashboard -> previous deployment -> "Promote to Production" (instant), then `git revert -m 1` or reset `main` to `4742a0e2` and push. No data to roll back - the fix branch writes nothing to the DB.

## H5 Option A (separate, only on Scott's go) - see `h5-load-index-provenance.md` §"Exact procedure"
0051 migration -> dry-run recheck -> `--apply --approved-by scott` -> merge `h5-option-a-runtime` -> deploy -> optional scalar re-stamp.

## Housekeeping after deploy
- `git worktree remove F:\clawd\wt-h5-runtime` once `h5-option-a-runtime` is merged or dropped (it junctions `node_modules` from the fix worktree).
- Kill the `:3002` preview (`calm-valley`) when Scott is done reviewing.
- Untracked evidence in the worktree (`docs/fitment-api/audit/retest/*.json`, `dev3002*.log`, `_commitmsg.txt`) is intentionally not committed.

## Addendum 11:20 - safety review findings -> fixes (HEAD 81caa6b1)

| Finding | Fix | Evidence |
|---|---|---|
| Q1-1 Critical: checkout dropped rearSku -> PO = FRONT x4 | `36301f57` `src/lib/checkout/buildCheckoutLines.ts` splits staggered lines front x2 + rear x2 (meta.axle, staggeredSetId, rear specs on rear line); availability pre-check covers both SKUs | `staggeredCheckoutContract.test.ts` "supplier PO built from the snapshot orders 2 front + 2 rear" asserts `extractItemsBySupplier` -> `[[FRONT,2],[REAR,2]]` |
| Q7-2 client price trusted at checkout | `src/lib/checkout/repriceCatalog.ts`: wheels WheelPros->TechFeed->Wheel-1->WSI, tires /api/tires/search exact part; server price charged; unpriceable/finish-mismatch -> 409 `line_unpriceable` (never $0 / client fallback) | tests "charges the SERVER price when the client price was tampered", "rejects when the rear SKU cannot be priced", "rejects ... finishes differ" |
| Q1-2/Q1-3 High: /wheels onSelect 4 x front, rear from base sku | cards pass authoritative `pair/setPrice/staggered`; grid uses exported `buildSelectedWheel()`; mobile Horizontal card had the same defect - fixed | tests "2 front + 2 rear from the card's pair, never 4 x front", "refuses a pair whose front SKU is a different variant" |
| Q1-4 cart merge / qty edits on 2+2 | `cartLineKey` = type|sku|rearSku; `isFixedQuantityLine` locks qty 4 on add + edit | tests "never merge", "fixed 2+2" |
| Q1-5 $0 x 4 staggered line | CTA disabled + `addToPackage` early return when rear unpriced | test "refuses an unpriced rear" |
| Q7-1 mojibake engine.ts/packageGap.test.ts | `81caa6b1` ASCII repaired, runtime `notes` string clean | `node docs/fitment-api/audit/retest/_fix-mojibake*.cjs` output: 0 non-ASCII remaining |
| Q6 8 suites not running (vitest imports) | `test-shims/vitest.ts` + jest moduleNameMapper | `jest-vitest-shim-8.txt`: 5 PASS (groupWheelsBySpec, tirePricingService, tireSizeUtils, staggeredCanonical, makeNormalization); universalFitmentResolver runs but its DB-backed cases are `skipped` (needs POSTGRES_URL); **still red**: `staggeredFitment` 3 stale assertions (diameterOptions now offers 24/26 chips flagged `hasInventory:false` since main `359b9cb9`, not a fit claim) and `safetyGovernor` (wheel-size.com governor - BANNED/disabled integration, `vi.mock` hoisting semantics; not deploy-relevant) |
| packageGap stale +/-5mm | assertions rewritten to the real +/-15mm loose pre-filter, geometry authoritative | 18/18 pass |
| Mach-E vs Mach 1 matching | reviewed: resolver + trim options use `ilike(model, variant)` with NO wildcards, alias table has no `mach` entry -> exact model match; my earlier ad-hoc `ILIKE '%mach%'` query caused the misattribution, not site code | code read `universalFitmentResolver.ts:612-622`, `canonicalResolver.ts:965+`, `modelAliases.ts getModelVariants` |

Behaviour change to flag: checkout now returns **409 `line_unpriceable`** when a wheel/tire SKU cannot be re-priced (e.g. TireWeb circuit open, supplier feed miss). This is the requested fail-closed rule; it can block a legitimate checkout during a supplier outage. Checkout page copy for that error is not yet written (shows generic failure) - UI follow-up.

`next build` on 81caa6b1: see `next-build-81caa6b1.log`.

## Release candidate c9e47109 - code-only, independent of H5 / DB cleanup (2026-09-19 12:40)

- Code HEAD `bfe2f072`; `c9e47109` adds evidence files only. Base `4742a0e2` = local `main`.
- `next build` EXIT 0 in 199 s on `c9e47109` (`next-build-c9e47109.log`, 1332 static pages).
- Full `jest --ci`: 29 suites / 685 tests pass; 4 suites / 25 tests fail (`jest-full-c9e47109.txt`), all classified NOT release-relevant:
  - `canonicalResolver.test.ts` (17) + `canonical-source.test.ts` (1): fail identically on `main` (`extractBmwModelAndTrim` no longer exported; stale contract) - baseline rot.
  - `staggeredFitment.test.ts` (3) + `safetyGovernor.test.ts` (4): legacy vitest suites that could not even load on `main` (`Cannot find module 'vitest'`); the shim runs them for the first time and exposes stale assertions against code the branch does NOT touch (26" inventory diameter now offered; wheel-size governor defaults). wheel-size feature is disabled in prod.
- Schema compatibility: 38/38 `vehicle_fitments` columns referenced by `schema.ts` exist in the live DB (read-only check `scripts/audit/_schema-vs-db.mjs`); `oem_load_index_by_size` is NOT referenced by any runtime file and NOT in the DB. Migration `0051` files are inert text - `npm run build` is plain `next build`, no migrate step. fitmentCache `CACHE_VERSION` v7 bumps keys forward; old keys expire naturally.
- Unpushed `main` (16 commits, `origin/main 85897aa0..4742a0e2`): 15 touch only `scripts/`, `docs/`, `tg-loop`; the one runtime commit is `bb7aad9c` (7 src files: expose `oemSpeedRating`, cache v5, schema comments). The branch is built on it and every build/test above exercised it. A code-only RC without `bb7aad9c` would be an untested variant (3 overlapping files) - not recommended.
- Live targets: Vercel project `warehouse-tire-site` -> shop.warehousetiredirect.com, shop.warehousetire.net, pos.warehousetiredirect.com. Rollback SHA: `85897aa0` (current `origin/main` = current production), or Vercel "promote previous deployment". No DB rollback needed.

## Isolated release candidate d3318ad6 (branch `release/fitment-audit-2026-09-19`, worktree F:\clawd\wt-release) - 2026-09-19 13:00

Built FROM `origin/main` (`85897aa0`, current production), NOT from local `main`. Contents: src-only extract of `bb7aad9c` (`54774587`) + the 39 reviewed fix-branch commits (cherry-picked, code tree byte-identical to `fix/fitment-audit-2026-09-18`) + `d3318ad6` below. The 15 unpushed scripts/docs/tg-loop commits on local `main` are excluded.

### d3318ad6 - checkout price authority on BOTH Stripe paths (found during this review)
- `/api/stripe/create-payment-intent` (embedded Payment Element = the default checkout page path, also LocalMobileCheckout) was charging the client-sent `unitPrice` for EVERY item (wheels, tires, accessories) and never split staggered sets - the earlier Q1-1/Q7-2 fix only covered `create-checkout-session`. Now routed through `buildCheckoutLines` + `defaultCatalogPriceResolver`; 409 `line_unpriceable` on failure.
- Accessories server-priced: fixed synthetic SKUs -> `accessories` (sell_price|msrp) -> `suspension_fitments` (msrp|map_price); unpriceable => reject. Only client-trusted value: `` for REQUIRED included hardware (lug/hub/valve categories) when catalog price <= `` or unknown. Road hazard recomputed from server tire prices (20%/tire, `` floor, qty = tires). Shared constants `src/lib/checkout/fixedPriceSkus.ts`.
- Tests: checkout contract suite 14 -> 24 (+8 accessory/road-hazard/TPMS/tire-meta, +2 route wiring guards). tsc 0.

### Validation on d3318ad6
- `next build` EXIT 0 in 217 s (`next-build-release-d3318ad6.log`).
- Full `jest --ci` (`jest-full-release-d3318ad6.txt`): suites 29 pass / 4 fail / 1 skipped; tests **695 pass**, 25 fail, 23 skipped. Failures are the same 4 pre-classified suites (canonicalResolver 17 + canonical-source 1 fail identically on main; staggeredFitment 3 + safetyGovernor 4 are legacy vitest suites that could not load on main and assert stale behaviour in code the release does not touch). Skipped = `src/lib/fitment/__tests__/universalFitmentResolver.test.ts` (all 23), gated `process.env.POSTGRES_URL ? describe : describe.skip` - a live-DB integration suite; jest runs without .env.local by design.
- Mocked fulfilment coverage: builder -> quote snapshot -> `extractItemsBySupplier` (WheelPros PO composition 2 front + 2 rear). NOT covered by tests: the webhook route handler itself (signature check -> getQuote -> createOrder -> processSupplierOrders -> email); it consumes the snapshot unchanged.
- Rollback: `85897aa0` (= origin/main = production) or Vercel promote-previous. No DB change in this release.

## Acceptance evidence pack for RC 3d95ec8d (code d3318ad6) - 2026-09-19 14:45

- **Baseline proof, per test:** the 4 failing suites were run with `--json` on a detached worktree of `origin/main` **85897aa0** (`F:\clawd\wt-base`, node_modules junction, vitest shim copied untracked so the two legacy suites can load) and on the RC. Both: 36 passed / **25 failed**, and the sorted failed `fullName` lists are **IDENTICAL** (`jest-4suites-base-85897aa0.json` vs `jest-4suites-rc-3d95ec8d.json`). Without the shim, base cannot load `staggeredFitment`/`safetyGovernor` at all (`Cannot find module 'vitest'`), i.e. 0 tests executed there on production today.
- **Changed-path tests:** `jest --findRelatedTests` over the 71 changed runtime `src` files -> 19 suites: **17 pass (357 tests)**, 1 fail (`canonicalResolver` 17 - related only through comment-only edits to `src/lib/fitment-db/schema.ts`; module under test unchanged; identical failures on base), 1 skipped (`universalFitmentResolver` 23, `POSTGRES_URL`-gated live-DB suite). `jest-related-rc-3d95ec8d.json`.
- **Schema compatibility (live, read-only):** every column the new code reads exists in production today (`vehicle_fitments`: wheel_specs_source, tire_sizes_source, load_index_source, source, certification_status, quality_tier, confidence_tag, display_trim, oem_load_index, oem_speed_rating; `accessories`: sell_price, msrp, category, sku; `suspension_fitments`: msrp, map_price, sku). `load_index_by_size` is MISSING and is referenced by **zero** runtime files (`git grep` = 0) - migration 0051 ships as files only, is not run by the build (`build: next build`, no vercel.json build hook, no drizzle migrate), and `schema.ts` differs from base by comments only. No package.json dependency changes.
- **Price authority probe against the live read-only catalog** (`scripts/audit/release-price-authority-probe.ts` -> `release-price-authority-probe-3d95ec8d.txt`): lift kit 44-3985 sent at `` -> server `.70 [accessories_db]`; same SKU relabelled `lug_nut required ` -> `.70`; lug nuts PHXNF25 required `` with catalog `.05` (> `` cap) -> charged `.05`; unknown SKU -> 409 `line_unpriceable`; TPMS x4 at `.01` -> `4 x .99 [fixed]`; road hazard with no tires -> 409; honest cart unchanged.
- **Preview** (read-only): `http://localhost:3002` on RC, `FITMENT_PREVIEW_READONLY=1 FITMENT_CACHE_DISABLED=1`, Upstash stripped, `POSTGRES_URL` has `default_transaction_read_only=on` (proof: `cannot execute UPDATE in a read-only transaction`). Live POST to `/api/stripe/create-payment-intent`: unknown SKU -> **409**; any priceable cart -> **500 `cannot execute CREATE TABLE in a read-only transaction`** at `createQuote` (route.ts:298), which is BEFORE `paymentIntents.create` (route.ts:388) - so the preview can never create a live Stripe object. The Stripe keys in the preview env are live-mode; the DB guard is what protects them.
- **Rollback target:** `85897aa0` (= origin/main = production). Vercel: promote previous deployment.
- Mojibake: `mojibake-diff` vs base introduces none in `src/`; `fitment-search/route.ts` carries 112 pre-existing on base. Two docs were de-mojibaked in this commit.

## Codex acceptance follow-up - included-hardware entitlement (commit 4f1b1740, 2026-09-19 16:30)

**Finding confirmed (RC 3d95ec8d/d3318ad6):** the `` included-hardware path trusted client `required`, `category` and `unitPrice`; a known `<= ` lug kit relabelled required, or any UNKNOWN SKU labelled `lug_nut`, could be taken for `` with no wheel purchase.

**Fix (`src/lib/checkout/buildCheckoutLines.ts`, `fixedPriceSkus.ts`, `repriceCatalog.ts`):** hardware kind from the SKU only (fitment-engine placeholders `LUGKIT-<parseable thread>` / `HR-<od>-<id>`, or `accessories.category` returned by the resolver); entitlement = wheel sets accepted in the same order (staggered pair = 1, square = ceil(qty/4)), one lug kit + one hub-ring set per set, unit qty <= sets; price cap `<= ` unchanged; unknown SKU always `unpriceable`; placeholder without entitlement -> `hardware_not_entitled` (409 via the existing `line_unpriceable` envelope on both Stripe routes). Client `` only orders which eligible line takes a free slot. `LUG-KIT-CHROME` 79.99 / `HUB-CENTRIC-RINGS` 24.99 added to fixed prices (cart upsell SKUs otherwise rejected at checkout); `CompleteYourSetup` imports the constants.

**Evidence:** contract suite 24 -> **30/30** (`jest-checkout-entitlement.json`); `tsc --noEmit` EXIT 0; live read-only probe `release-price-authority-probe-entitlement.txt` (relabelled lift kit charged `.70`; PHXNF25 `.05` charged over cap; unknown SKU 409; placeholder w/o wheels 409 `hardware_not_entitled`; legit placeholder pair w/ wheel set ``; 3 kits for 1 set 409; LUG-KIT-CHROME `.99`); **full `next build` on 4f1b1740 EXIT 0** (`build-4f1b1740.log`, 183 s). Preview restarted on :3002; `/cart` and `/package/customize` 200; staggered RC7 2+2 + placeholder lug kit POST builds lines and stops at the read-only `createQuote` guard before Stripe.

**Staggered acceptance pair (preview):** `http://localhost:3002/wheels?year=2020&make=Ford&model=Mustang&trim=GT%20Performance%20Pack` -> ROHANA RC7 Silver Machined, front `RC719855114MS15` 19x8.5 +15 `.70`, rear `RC719955114MS20` 19x9.5 +20 `.70`, set `.80` (2+2).

## Codex browser-acceptance follow-up - customer-facing release blockers (2026-09-19 22:00, on 71da4ded)

**Browser acceptance (Scott's Chrome via chrome-relay, read-only preview `next start -p 3002`, build `x1CxPuYes0VJ2F4zvEQ0E`):** `/wheels` Mustang GT PP -> ROHANA RC7 staggered 2+2 -> cart -> checkout rendered; 8 negative probes against `create-payment-intent` all rejected before Stripe (0 PaymentIntents). Full pack: `memory-temp/accept-evidence-2026-09-19.md` (workspace, not committed). Slow-page root cause was Node stdout redirected to a file (synchronous on Windows) plus ~1.5 MB of `console.log` per catalog call; relaunching with stdout to NUL took `/wheels` from 46 s to 7 s. No code change for that.

**Findings Codex classed as release blockers, and the fixes (this commit):**

| # | Finding | Fix |
|---|---|---|
| 1 | Cart and both checkout Order Summaries showed only the FRONT size/SKU of a staggered wheel line ("19x8.5 - Qty 4", one SKU); the rear 19x9.5 ET20 / rear SKU were invisible at payment. | `cart/page.tsx` `WheelCartItem`: staggered lines list Front x2 / Rear x2 with diameter, width, ET and SKU per axle, bolt pattern, fixed "4 (2 front + 2 rear)" quantity (no dropdown; `CartContext` already pinned it), and "2 x front + 2 x rear" price split. `checkout/page.tsx`: new `WheelSummaryDetails` used by BOTH the mobile collapsible and the desktop summary (same content). |
| 2 | Checkout value-framing bullets were static: "Fitment verified for your <vehicle>", "Everything matched specifically", "Complete wheel & tire package" printed for every order, including one whose only line is `fitVerified: false` and whose cart says "fit not yet confirmed". | Bullets derived from the cart: package bullet only when wheels AND tires are present; "verified"/"matched" only when every vehicle-tagged wheel/tire line has `fitVerified === true` (the certified add-to-cart flag, same rule as the cart pill); otherwise the neutral "Selected for your <vehicle> - fit not yet confirmed" (`data-testid="checkout-fit-unconfirmed"`). |
| 3 | Both Stripe routes returned the raw exception text in the 500 body (a pg message reached the browser). | `src/lib/checkout/responses.ts`: `checkoutFailureResponse` -> `{error:"checkout_failed", detail:<generic>, ref:"chk_..."}`; the ref and the real error go to `console.error` only. `rejectedLinesResponse` sanitises 409 bodies (reason/sku/axle/name; server-side `detail` logged, not sent) and uses one wording per rejection class. Checkout UI now shows `detail` before the machine `error` code. |
| 4 | Placeholder hardware entitlement accepted any SKU matching the placeholder FORMAT (`HR-99-99` with a wheel set was entitled): dimensions came from the client-formatted SKU, never from the selected wheel/vehicle. | `src/lib/checkout/hardwareSpec.ts` (new): server derives the vehicle thread / seat / hub bore from the fitment DB (`resolveUniversalFitment`, real `vehicle_fitments`/legacy rows only - never fallback) and the selected wheel's center bore from WheelPros -> TechFeed -> Wheel-1 -> WSI. `buildCheckoutLines(items, priceResolver, hardwareResolver)` validates every placeholder against them: lug thread must equal the vehicle thread; hub ring must equal `calculateHubRingSpec(vehicleHub, wheelBore)`. Any disagreement -> `hardware_mismatch` (detail `expected LUGKIT-M14x1.5` / `expected HR-73-71`, no ring needed, wheel bore smaller than hub); anything the server cannot derive (no vehicle on the wheel line, hardware's `wheelSku` not in the order or ambiguous, fitment record or catalog silent) -> `hardware_unverifiable`. Both BLOCK the checkout (409 `hardware_unverified`, customer wording tells them to re-add the wheels). The server-derived spec is stamped on the line meta (`hardwareSpec`) for fulfilment. |

**Evidence:** `jest src/lib/checkout` **43/43** (new `hardwarePlaceholderValidation.test.ts`: match, imperial thread, thread mismatch, ring mismatch by wheel bore and by vehicle hub, no-ring-needed, bore-smaller-than-hub, each unverifiable cause, wheel-not-in-order/ambiguous, rear-SKU reference, resolver receives FRONT sku + vehicle, price authority unchanged, response sanitisation, generic 500 with logged ref; contract-suite wiring guard now asserts the hardware resolver and the shared responses on both routes). `tsc --noEmit` EXIT 0. **Full `next build` EXIT 0** (`memory-temp/build-codexfix-prodpreview.log`, 394 s, BUILD_ID `Cfo8EcNPnxbCDy6onPJoE`). Preview restarted (stdout to NUL). Live read-only probes on the rebuilt preview (`memory-temp/accept-probe3.mjs`, `accept-probe4.mjs`): genuine Mustang/RC7 cart -> placeholders VALIDATED by the server (fitment DB gave M14x1.5 / 70.5 mm, WSI gave 73.1 mm bore -> `HR-73-71`), tampered $1 repriced to 466.70/505.70 then stopped by the read-only DB with a generic 500 + `ref=chk_...` (raw pg text only in the server log); `HR-99-99` -> 409 `hardware_mismatch` expected `HR-73-71`; `LUGKIT-M12x1.5` -> 409 mismatch expected `LUGKIT-M14x1.5`; hardware pointing at a wheel not in the order -> `hardware_unverifiable`; wheel line without a vehicle -> both placeholders `hardware_unverifiable`; `create-checkout-session` same envelope. 0 PaymentIntents / 0 Checkout Sessions created. Browser retest: cart shows "Front x2: 19 x 8.5 ET15 SKU RC719855114MS15 / Rear x2: 19 x 9.5 ET20 SKU RC719955114MS20", qty "4 (2 front + 2 rear)", "2 x $466.70 front + 2 x $505.70 rear"; checkout summary shows the same per-axle lines and "2 x $466.70 + 2 x $505.70", total $1944.80 unchanged; zero static "verified"/"matched" strings on the page, neutral line present (`memory-temp/accept-cart-2.png`, `accept-checkout-2.png`).

**Observed, not changed (for Codex):** the `/wheels` card says "Verified Fit" for this RC7 pair while the cart line arrives with `fitVerified` unset, so cart and checkout say "fit not yet confirmed" for a set the wheels page certified. The add-to-cart path (`buildSelectedWheel` / `WheelsStyleCardHorizontal`) does not carry `fitBadgeAllowed` into `fitVerified`. Under-claim, not a false claim; flagged for a decision, left out of this bounded fix.

## Codex review of 80bf3a69 - hub-ring precision + fit-claim evidence (2026-09-20 ~07:40)

**Finding 1 (confirmed): hub-ring placeholder collapsed tenths to whole mm.** `HR-${outer.toFixed(0)}-${inner.toFixed(0)}` in the cart generator (`useAccessoryFitment.ts` x2, `accessories.ts`) and the same rounding in the server comparison. 73.1→70.5 became `HR-73-71`, which is also what 72.6→71.4 (a different physical ring) produces; the server accepted any placeholder whose rounded digits matched.

**Fix (end to end):**
- `accessories.ts`: `formatHubRingMm` (always one decimal) + `formatHubRingSku` -> `HR-73.1-70.5`. Every generator uses it (both hook paths, the accessories service placeholder).
- `fixedPriceSkus.ts`: placeholder regex accepts `\d{2,3}(\.\d)?`; `parseHubRingPlaceholder` returns `{outer, inner, tenths}`. Whole-mm SKUs (pre-2026-09-20 carts in localStorage) still parse so they reach the comparison and are rejected as `hardware_mismatch`, not silently `unpriceable`.
- `buildCheckoutLines.ts`: expected = `formatHubRingSku(calculateHubRingSpec(vehicleHub, wheelBore))`; accept only when the claim is in tenths AND both dimensions are within 0.05 mm of the derived ring. Accepted lines carry `meta.hardwareSpec = {outerDiameterMm, innerDiameterMm, wheelBoreMm (unrounded), vehicleHubMm (unrounded), wheelSku, sources}` and the line NAME fulfilment sees is now server-derived (`Hub Centric Rings 73.1mm -> 70.5mm (set of 4) - Included`, `Lug Kit M14x1.5 (conical seat) - Included`) instead of the client string.
- Regressions (`hardwarePlaceholderValidation.test.ts`): tenths formatting incl. 2-decimal catalog bores; **collision regression** (73.1/70.5 vs 72.6/71.4: same whole-mm SKU before, distinct now; both whole-mm and the colliding ring rejected against the derived spec); tenth-off on either dimension rejected; 73.12/70.48 accepted as `HR-73.1-70.5` with unrounded inputs stamped; **generator parity** (`calculateAccessoryFitment` client output == the SKU the server accepts). Contract suite fixtures moved to tenths.

**Finding 2 (traced): wheels card "Verified Fit"/"Good Fit" vs cart `fitVerified` unset.** Root cause: `fitVerified` (added 2026-09-18 for the cart copy) had NO producer anywhere - no add-to-cart path set it. The evidence does exist: fitment-search returns per-SKU `fitmentValidation.certified` (= `isCertifiedFit(geometryClass, certificationBlock)`, i.e. profile certified for THIS vehicle AND geometry surefit/specfit); `/wheels` dropped it when mapping items (the prop-pass-through trap). Second finding while tracing: a staggered card showed the FRONT SKU's class for the whole pair - ROHANA RC7 front 19x8.5 ET15 = specfit/certified, rear 19x9.5 ET20 = extended (ET20 > allowed max offset 15), yet the card said "Good Fit". Third: checkout's "Fitment for <vehicle>" chip used `getFitmentMessaging(wheels[0].fitmentClass)`, which DEFAULTS to "Good Fit" when the class is unknown and never looked at certification; the cart's class badge ("Good Fit") likewise ignored `fitVerified`.

**Fix (evidence-derived, never copied true):**
- `fitCertification.ts`: `pairFitmentClass(front, rear)` = weaker axle (unknown rear -> extended, unknown front -> no badge); `pairCertified(f, r)` = both true. Unit-tested.
- `/wheels` page: builds `certifiedBySku` / `classBySku` from the response; each item's `fitmentClass` (for a staggered front) = `pairFitmentClass`, `fitCertified` = own certified (square) or `pairCertified` (staggered). Passed through `groupWheelsBySpec` (group certified only when EVERY finish variant is; tested), `WheelItem`, `SelectedWheel`/`buildSelectedWheel` (tested), and all five cart constructions (grid square/staggered x2, `WheelsStyleCard.addItem`, `WheelsStyleCardHorizontal.addItem`): `fitVerified = fitCertified && !certificationBlock`.
- Cart: class badge shown only when `fitVerified`; checkout chip shows the class only when every vehicle line is verified, else neutral "Fit not yet confirmed" (`data-testid="checkout-fit-chip"`).

**Evidence (read-only preview `next start -p 3002`, build `C5uqgF1cMLWm5in5Z_7hq`):** `jest` checkout + groupWheelsBySpec + fitCertification **80/80**; `tsc --noEmit` EXIT 0; `next build` EXIT 0 (`memory-temp/build-codexfix2-prodpreview.log`; note: 1 GB heap OOMs on this box now, 2 GB builds in ~2 min). Browser (Scott's Chrome via chrome-relay):
- ROHANA RC7 Matte Graphite F `RC719855114MG15` 19x8.5 ET15 / R `RC719955114MG40` 19x9.5 ET40 (server: front specfit certified, rear extended): card **CUSTOM FIT** (was Good Fit) -> cart line `fitmentClass: "extended", fitVerified: false`, no class badge, "For 2020 Ford Mustang · fit not yet confirmed"; checkout chip "Fit not yet confirmed", 0 verified/matched strings; hub-ring placeholder `HR-73.1-70.5` (was HR-73-71). Screens `accept-cart-5-unverified.png`, `accept-checkout-5-unverified-chip.png`.
- RIDLER 652 F `652-2865GBD` 20x8.5 / R `652-2165GBD` 20x10 (server: BOTH specfit certified): card SPEC FIT -> cart `fitVerified: true`, "Good Fit" badge + "Fits 2020 Ford Mustang"; checkout chip "✓ Good Fit" + "✔ Fitment verified for your 2020 Ford Mustang". Hub ring `HR-83.8-70.5` from a **83.82 mm** WheelPros bore (tenths rounding exercised live). Screens `accept-cart-4-verified.png`, `accept-checkout-4-verified.png`.
- Server probes (`memory-temp/accept-probe5.mjs`, `accept-probe6.mjs`; 0 Stripe calls): genuine RC7 cart -> placeholders validated (server derived `HR-73.1-70.5` from fitment DB 70.5 + WSI 73.1), stopped by read-only DB with generic 500 + `chk_` ref; legacy `HR-73-71` -> 409 `hardware_mismatch` (log: expected HR-73.1-70.5); collider `HR-72.6-71.4` -> 409; tenth-off `HR-73.2-70.5` -> 409; tampered client `spec`/`name` with intact SKU -> ignored (server spec + name win); `create-checkout-session` same; RIDLER genuine `HR-83.8-70.5` validated, legacy `HR-84-71` -> 409 (log: expected HR-83.8-70.5).

**Observed, not changed:** (a) the envelope's allowed offset for this Mustang is -25..+15 because the OE offset is unknown (recorded as 0), so most positive-offset rears classify `extended` - a fitment-DB data gap, not a code path; it under-claims. (b) Tire add-to-cart paths also never set `fitVerified` (tires already gate their badge on `fitBadgeAllowed`); checkout therefore never claims "verified" for a wheels+tires cart. Under-claim; wiring it is analogous to this change and left for a follow-up. (c) The rear item's own `pair.role` reads "front" in the API response (label only; pairing/pricing unaffected).

## Codex review of fba009bb - "OE offset = 0" trace and gate fix (2026-09-20 ~09:00)

**Question:** is the 2020 Mustang GT Performance Pack "OE offset 0" sourced or a missing-value default, and can it falsely certify a wheel?

**Exact row (read-only, `memory-temp/ro-mustang-offset.mjs`):** `vehicle_fitments.id = bb27a4b8-d09b-44d3-9e8f-86c9c4866690` (2020 ford mustang, display_trim "GT Performance Pack", modification_id `ford-mustang-gt-performance-pack-1db4ea26c6`, source `trim-research`, wheel_specs_source `xref:wheelpros`, wheel_specs_verified_at 2026-09-16). `offset_min_mm = 30.00`, `offset_max_mm = 52.00` (SOURCED). `oem_wheel_sizes = [{axle:front, 19x9, offset:null}, {axle:rear, 19x9.5, offset:null}]` - every inline offset is NULL. Nothing in the row says 0.

**Source path of the 0:** `src/lib/aftermarketFitment.ts buildFitmentEnvelope()` read ONLY `oem.wheelSpecs[].offset` (the inline values) and, when none existed, set `oemMinOffset = oemMaxOffset = 0` as a placeholder ("so classification doesn't break", 2026-06-30 note) - the DB range was never passed in (`OEMSpecs` had no field for it). `validateWheel()` then classified against `oem [0,0]` / `allowed [-25,+15]`, and ranking used `oemMidOffset = 0`. Reproduction on the pre-fix preview (`memory-temp/ro-mustang-api.mjs`): `fitment.envelope.oem.offset = [0,0]`, `allowed.offset = [-25,15]`; 201 wheels with ET inside the true factory 30..52 -> 190 "extended", 11 specfit; RIDLER 652 20x8.5 ET0 / 20x10 ET0 -> specfit + `certified:true`; RC7 19x8.5 ET15 -> specfit certified.

**Why the safety gate did not catch it:** `resolveOemOffset()` (geometryValidator.ts) DID find the DB range (midpoint 41, `db_range_midpoint`), so the route's "missing OEM offset -> no results" gate passed. But for a known-staggered vehicle the route resolves per-axle with `requireAxleSpecific:true`; with inline offsets null both axles return `{missing:true}`, and the geometry block in the candidate loop is guarded by `if (activeOemGeo && !activeOemGeo.missing)` - so it was SKIPPED for every candidate. Result: a 20x10 ET0 rear (delta_outboard ~47 mm vs the 9.25"/ET41 reference; aggressive-car ceiling 35 mm) was never gated and was certified. **Yes - unknown (unsourced) data certified wheels, it was not merely an under-claim.**

**Scope (read-only counts, `ro-offset-scope.mjs`):** 32,569 certified live rows; 14,058 have inline offsets; **18,511 have ONLY the DB range** (envelope defaulted to 0 for all of them); 0 have no offset at all; 0 inverted ranges; 0 half-null ranges; 0 rows with a genuine 0..0 range. Staggered-capable models affected by the skipped gate include Porsche 911 (167 rows), Cayenne, Corvette (104), GT-R, Panamera, Camaro (52), M3, 370Z, M5.

**Fix (commit below):**
- `aftermarketFitment.ts`: `OEMSpecs.offsetMinMm/offsetMaxMm`; envelope precedence inline offsets (finite only) -> DB range -> UNVERIFIED (`oemOffsetSource`: `oem_wheel_sizes | db_offset_range | conflicting | unverified`). Inverted range (min > max) = `conflicting`, not normalised. `validateWheel`: unverified/conflicting offset -> `offsetInRange:false`, major deviation -> `extended` (never surefit/specfit) with reason; a wheel whose own width/offset/diameter/bore is unknown -> `extended` (was specfit). A genuine 0 mm offset (inline 0 or range 0..0) is verified like any value and can still be surefit.
- `fitment-search/route.ts`: passes the row's range into the envelope (DB-first and legacy paths); when a staggered axle has no per-axle reference the geometry gate runs against the sourced midpoint INSTEAD of being skipped, AND the candidate is capped at `extended` with reason "Factory front/rear offsets not known per axle - staggered fit cannot be certified" (a shared range can hide different front/rear OE offsets, so the midpoint may gate browsing but never justify a certified per-axle fit). Response adds `fitment.oemOffset = {source, verified, geometryReference, frontAxleSpecific, rearAxleSpecific}`.
- Tests: `src/lib/fitment-db/__tests__/oemOffsetProvenance.test.ts` (Mustang row fixture; RIDLER ET0 extended / factory-like ET40 certified; inline wins over range; genuine 0 verified; no data -> extended not excluded; NaN/Infinity/undefined ignored; inverted range conflicting; wheel-side unknown offset -> extended; per-axle missing -> midpoint reference exists and RIDLER 20x10 ET0 fails aggressive while 19x9.5 ET45 passes). `fitmentRegression.test.ts` unchanged and green.

**Evidence (rebuilt read-only preview, BUILD_ID `_T3sja_yR0YMyW3Qot-xD`, `memory-temp/ro-offset-retest.mjs`):**
- Mustang GT PP: envelope `oem.offset [30,52]`, `allowed [5,67]`, `oemOffset.source db_offset_range`, `geometryReference db_range_midpoint`, `frontAxleSpecific:false`; total 2901 -> 2412 (gate now excludes); **certified 0** (all `extended`); RIDLER 20x10 ET0 rear **excluded**, 20x8.5 ET0 front browses as a square extended item; RC7 pair extended. Browser: 24 cards all CUSTOM FIT, 0 SURE/SPEC (`accept-wheels-6-offset-fix.png`).
- Santa Fe 2016 Base (non-staggered, range 35..55 only): envelope 35..55, 500/500 certified (122 surefit / 378 specfit, ET 20..50) - the fix does not empty non-staggered vehicles; it re-centres them on the factory offset (browser: 24 SURE FIT).
- Shelby GT350 2020: resolver picked row `0f048d78` (trim-research, inline offsets null) over duplicate `31487be1` (tiresize.com, inline 35/52) -> all extended. Duplicate-row data issue, flagged.
- jest 6 suites 109/109; `tsc` EXIT 0; `next build` EXIT 0 (`memory-temp/build-codexfix3-prodpreview.log`). Baseline-failing suites unchanged: `staggeredFitment.test.ts`, `canonicalResolver.test.ts` (20 failures identical with the change stashed; `extractBmwModelAndTrim` missing export - pre-existing).

**Customer-visible consequence (needs Scott's eyes):** every staggered vehicle whose row lacks per-axle inline offsets (Mustang GT PP, Camaro, Corvette, 911, M3...) now shows **Custom Fit** on all wheels until per-axle OE offsets are sourced (Tire Guide prints carry them). Non-staggered vehicles keep certification, now against the factory range.

**Still open (queued browser findings, not touched):** the wheels-page header pill `src/app/wheels/page.tsx:1790-1793` says "Verified Fit" from `fitment.confidence === "high"` even when 0 results are certified; static trust strip "Guaranteed Fit" in `BuildTypeSelector.tsx:734`; `FitmentConfidenceBadge.tsx:43` "Verified Fitment". Runtime logs `docs/fitment-api/audit/dev3002*.log` untracked + ignored in this commit (history retains them).


## RELEASE PACK - RC d05288df (release/fitment-audit-2026-09-19) - 2026-09-20 14:35 ET

**Codex acceptance chain (all independent):** f67b3441 supplier identity (51/51), fcc7b269 fees, d05288df stale-PI client lifecycle (3 suites/53 PASS on diff inspection), browser acceptance on preview build `Wgw1XE9mDWbNOf1l3i_zj` PASS (package/review front 20x8.5 652-2865GBD + rear 20x10 652-2165GBD, 2+2, 2x336.12 + 2x345.41 = 1363.06; no false verified chip; cart + checkout same total/axles, "fit not yet confirmed"; no POST/payment). `aae9a72c` is NOT accepted on its own; its cancellation/webhook additions are reverted by `6033ea04` (webhook byte-identical to base).

**Exact contents:** 65 commits `54774587..d05288df` on top of **base `85897aa0` = origin/main = current production** (merge-base = origin/main, 0 behind). First commit `54774587` is the src-only extract of local-main `bb7aad9c`; local `main` (`435ce658`, 17 unpushed commits: TG-loop, scripts, docs, audit artifacts) is **excluded and must not be pushed as-is**. Runtime diff vs base: 129 `src/` files, +13,626/-3,076. `package.json`/`package-lock.json`: 0 lines changed. `.gitignore`, `jest.config.js`, `test-shims/vitest.ts` (test-only).

**Gates on d05288df (this HEAD, not an earlier RC):**
- `next build` EXIT 0, "Compiled successfully", 208 s, BUILD_ID `Wgw1XE9mDWbNOf1l3i_zj` - `build-d05288df.log`. Served by the read-only preview :3002 (pid 17528, `next start`) that Codex browser-accepted; `.next/BUILD_ID` on disk matches, `git status` clean at build time.
- `tsc --noEmit` EXIT 0 (at d05288df).
- Full `jest --ci --json` (`jest-full-release-d05288df.json`): **39 suites / 855 tests pass**; 4 suites / 25 tests fail; 1 suite / 23 tests skipped. The failing set is the SAME 4 suites and the same 25 tests as the per-test baseline proof against base `85897aa0` recorded 2026-09-19 14:45 (`canonicalResolver` 17, `staggeredFitment` 3, `safetyGovernor` 4, `canonical-source` 1 - stale contracts / vitest-in-jest, none release-relevant); skipped = `universalFitmentResolver` (`POSTGRES_URL ? describe : describe.skip`; covered by the fitment-search / certification suites). Passing tests grew 695 -> 855 with the checkout/supplier/session suites added since.
- Price authority probe on the live catalog, read-only (`release-price-authority-probe-d05288df.txt`): every tampered accessory/hardware price is re-priced from the catalog or rejected 409 (`unpriceable`, `hardware_not_entitled`, `hardware_unverifiable`); no client price honoured.
- Schema: the release reads no column absent from production. `drizzle/migrations/0051_*` (H5 Option A: `oem_load_index_by_size`, `oem_speed_rating_by_size`, `load_index_by_size_source`, `load_index_by_size_verified_at`) ships as files only - `git grep` over `src/` = 0 references for each; `next build` runs no migrate hook. **No DB change in this release.** DB batch `f194cbc` and H5 stay HELD.
- Mojibake: `mojibake-diff` vs base introduces none in `src/` (`fitment-search/route.ts` keeps its 112 pre-existing log-string cases from base). The only "introduced" hit is a captured response body under `docs/fitment-api/audit/retest/` (a 19"/20" label emitted by that pre-existing route mojibake) - artifact, not code.
- Read-only preview safeguards were proven, not assumed: `default_transaction_read_only = on`, `UPDATE` blocked; `STRIPE_SECRET_KEY` overridden to a dummy in the server env; Upstash stripped; `tireweb_sku_cache` unchanged (35,730 / 2026-09-19T23:17).

**Destination:** GitHub `scott48342/warehouse-tire-site` `main` -> Vercel project `warehouse-tire-site` -> shop.warehousetiredirect.com, shop.warehousetire.net, pos.warehousetiredirect.com (auto-build from `main`).

**Deploy command (Scott owns push; fast-forward, leaves local `main` untouched):**
```powershell
cd F:\clawd\wt-release
git fetch origin; git merge-base --is-ancestor origin/main d05288df; $LASTEXITCODE   # must be 0 (still a fast-forward)
git push origin d05288df:refs/heads/main
```
Push `d05288df` **directly to main** rather than pushing the release branch first: Vercel's Preview environment shares production `POSTGRES_URL` (writable), live Stripe keys and Upstash with no read-only/no-payment guard (checked 2026-09-19), so a branch push would run an unguarded preview for no gain - the local read-only preview already carried the browser acceptance. Afterwards local `main` needs `git rebase origin/main` (or Scott's chosen reconciliation) before any of its 17 commits go out.

**Rollback:** Vercel dashboard -> previous deployment (`85897aa0`) -> Promote to Production (instant); or `git push origin 85897aa0:refs/heads/main --force-with-lease=main:d05288df`. No data to roll back.

**Post-deploy smoke (read-only, 5 min):** the list in "Exact deployment procedure" step 4 above, plus: `/api/wheels/fitment-search?year=2020&make=Ford&model=Mustang&trim=GT%20Performance%20Pack&diameter=20` -> `fitment.oemOffset.source = db_offset_range`, `frontAxleSpecific = false`, no card certified; `POST /api/stripe/create-payment-intent` with a tampered total -> 409 `totals_changed` (no PI created); checkout page: change an input while a PI is loading -> Payment Element hides, no stale intent id installed.
