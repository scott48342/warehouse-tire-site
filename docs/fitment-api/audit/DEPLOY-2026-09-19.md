# Fitment audit fix branch — blockers & deployment procedure (2026-09-19 10:50 ET)

Branch: `fix/fitment-audit-2026-09-18` @ `b332088c` (worktree `F:\clawd\wt-fitment-fix`), 31 commits on top of
`main` @ `4742a0e2` (which itself is 16 commits ahead of `origin/main`, unpushed). Separate, NOT to deploy yet:
`h5-option-a-runtime` @ `f65f0e9a` (needs migration 0051 first).

## Remaining blockers (actual)

| # | Blocker | Owner | What unblocks |
|---|---|---|---|
| 1 | **Reviewer acceptance** of the Jake batch (`40860ce8`/`8c3b76f2`) and of today's commits (`09e18766` stagger/load-scope, `f6db6174` packages, `b332088c` tone). Scott's browser pass so far: J2/J4 refusals PASS, Mach-E chips PASS, HD 6x139.7 rejection PASS, tone "still verbose" (addressed in `b332088c`, untested). | Scott | Scott's "accepted" |
| 2 | **Nothing is pushed.** `main` is 16 ahead of origin; the fix branch has never been pushed. Vercel builds from GitHub. | Scott (declined to delegate pushes) | `git push origin main` + push the branch (see procedure) |
| 3 | **Jake still suggests "check the door jamb for bolt pattern"** on HD (door jamb has tire size/pressure, not bolt pattern). Prompt-only fix, not yet done. | me, ~10 min | say go; folded into the same deploy |
| 4 | **Mach-E 2022 "Base"/"Handling Package" rows** carry gas-Mustang / GT500 data (5x114.3, 255/40R19, 305/30R19) under the Mach-E model. Gate marks them unverified, but they are wrong rows. Bolt field + only 1 approved source (TG Pro) → outside my standing approval. | Scott | approve quarantine of those 4 rows (`6dc2e084`, `463d44a3`, `8302a4ef`, `b6dce2ba`) or a TG Pro re-check |
| 5 | **H5 Option A decision** — migration 0051 + backfill (28,268 rows) + runtime branch. Fully prepared; nothing applied. Until then the interim `per_axle_unknown` rule (live on the branch) keeps every multi-size/staggered vehicle package-ineligible with no minimum shown — safe, but it removes package recommendations from ~16k rows. | Scott | "go" on the 5-step procedure in `h5-load-index-provenance.md` |
| 6 | **Perf, not correctness:** `/wheels?…&package=1` on the M4 renders 782 staggered pairs (`pageSize=2000`), 31 s server render, 1.3 MB HTML. Pre-existing page design; H2 pairing made every finish a pair. Suggest a follow-up: cap pairs per style or paginate. | later | separate task |
| 7 | Jest baseline: 11 suites fail identically on `4742a0e2` (vitest-in-jest imports, packageGap ±5 mm bound, etc.). Not introduced here; not fixed here. | later | separate cleanup |

Not blockers: `packages/recommended` returns 0 packages for source-unverified vehicles (Camry, Raptor) — that is the
R3 gate from `1e4281de` working as designed; Mach-E GT (TG Pro) builds one, flagged estimated/not certified.

## What the branch changes at runtime (for the deploy risk read)
- Fit certification fails closed on: trim ambiguity, unapproved source provenance, mixed-diameter requests on
  square/unknown OE vehicles, non-OE sizes (`direct:`, plus-sizing, lifted, classic upsize).
- Load-index minimum asserted only for single-OE-size records; otherwise no minimum, package-ineligible.
- Staggered wheels: same-finish front/rear pairing, 2+2 pricing everywhere, both axles handed to tire search.
- Packages: estimated/never certified/never directly purchasable; LT-aware; no NaN/fabricated sizes.
- Copy: no "Guaranteed"/"Fits" without `fitBadgeAllowed`; Jake refuses to "confirm" unverified specs.
- Cache: fitment profile cache key **v7** (v8 only on the H5 runtime branch) — old entries are simply missed, no flush needed.
- **No new env vars for production.** `FITMENT_PREVIEW_READONLY` / `FITMENT_CACHE_DISABLED` are preview-only switches, unset in Vercel = off.
- **No migration required** for the fix branch. 0051 is on the branch as a file only; nothing reads the new columns until `h5-option-a-runtime` merges.
- No supplier/order-path code touched (Stripe webhook, USAF/WheelPros order clients untouched — `git diff 4742a0e2..HEAD --stat -- src/lib/suppliers src/lib/usautoforce src/lib/wheelpros/orderClient.ts src/app/api/webhooks` is empty).

## Exact deployment procedure (after Scott's acceptance; standing deploy authorization applies)

Targets: GitHub `scott48342/warehouse-tire-site`, Vercel project `warehouse-tire-site` → production domains
`shop.warehousetiredirect.com`, `shop.warehousetire.net`, `pos.warehousetiredirect.com` (one project, deploys from `main`).

1. **Final gate in the worktree** (serial, ~4 min):
   ```powershell
   cd F:\clawd\wt-fitment-fix
   npx tsc --noEmit -p tsconfig.json          # expect exit 0 (last: 0 at b332088c parent)
   npx jest --silent 2>&1 | Select-String "Tests:"   # expect 19 failed (baseline) / rest pass
   npm run build                                # next build - NOT yet run on this branch (memory: run alone)
   ```
2. **Merge into main** (fast-forward is impossible — branch is based on `4742a0e2` = main HEAD, so it IS a fast-forward):
   ```powershell
   cd F:\clawd\warehouse-tire-site
   git status --short        # must be clean of unrelated edits, or stash them (Scott's call - main has TG-loop work)
   git merge --ff-only fix/fitment-audit-2026-09-18
   git log --oneline -1      # b332088c
   ```
3. **Push** (Scott): `git push origin main`. Vercel auto-builds production from `main`. Watch the build in the Vercel dashboard; a failed build leaves the previous deployment live.
4. **Post-deploy smoke (5 min, read-only)** against `https://shop.warehousetiredirect.com`:
   - `/api/tires/search?year=2022&make=Toyota&model=Camry&modification=SE&wheelDiameter=18&rearWheelDiameter=20&pageSize=5` → `certificationBlock: "aftermarket_stagger"`, `staggeredSearch.oemStaggered: false`.
   - `/api/tires/search?year=2020&make=Chevrolet&model=Corvette&modification=Stingray&wheelDiameter=19&rearWheelDiameter=20&pageSize=5` → `oemStaggered: true`, `loadRequirement.scope: "per_axle_unknown"`, every result `packageEligible: false`.
   - `/api/tires/search?year=2024&make=Ford&model=Mustang&modification=GT&wheelDiameter=19&rearWheelDiameter=20&pageSize=5` → `certificationBlock: "stagger_unverified"`.
   - `/api/wheels/fitment-search?year=2024&make=BMW&model=M4&trim=Competition%20xDrive&package=1&pageSize=50` → pairs have `front.finish === rear.finish`, `setPrice === 2*front.price + 2*rear.price`.
   - `/api/packages/recommended?year=2022&make=Ford&model=Mustang%20Mach-E&trim=GT` → `packages[0].priceEstimated === true`, `certified === false`.
   - `/api/fitment/lifted?year=2020&make=Ford&model=F-150&trim=XLT&liftInches=4` → `liftLevel.id: "offroad"`.
   - Jake: one chat "2024 BMW M4 Competition xDrive bolt pattern?" → must not say "confirmed"/"both sources".
   - `npm run fitment:smoke-test` (existing production smoke) if it still passes on baseline.
5. **Rollback**: Vercel dashboard → previous deployment → "Promote to Production" (instant), then `git revert -m 1` or reset `main` to `4742a0e2` and push. No data to roll back — the fix branch writes nothing to the DB.

## H5 Option A (separate, only on Scott's go) — see `h5-load-index-provenance.md` §"Exact procedure"
0051 migration → dry-run recheck → `--apply --approved-by scott` → merge `h5-option-a-runtime` → deploy → optional scalar re-stamp.

## Housekeeping after deploy
- `git worktree remove F:\clawd\wt-h5-runtime` once `h5-option-a-runtime` is merged or dropped (it junctions `node_modules` from the fix worktree).
- Kill the `:3002` preview (`calm-valley`) when Scott is done reviewing.
- Untracked evidence in the worktree (`docs/fitment-api/audit/retest/*.json`, `dev3002*.log`, `_commitmsg.txt`) is intentionally not committed.
