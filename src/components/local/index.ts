/**
 * Local Mode Components
 * 
 * CRITICAL: These components should ONLY be used in local mode.
 * They are wrapped with <LocalOnly> guards internally, but for
 * code clarity, avoid importing them in national-only code paths.
 */

export { StoreSelector, SelectedStoreBadge, StoreInfoCard } from './StoreSelector';
